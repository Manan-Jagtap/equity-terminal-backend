# Local dev uses SQLite automatically (no setting needed).
# For production (Railway/Render) set the Postgres URL they give you:
# DATABASE_URL=postgresql://user:pass@host:5432/dbname
# Lock CORS to your deployed frontend once live:
# FRONTEND_ORIGIN=https://your-app.vercel.app
